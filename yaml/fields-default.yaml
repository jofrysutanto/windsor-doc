type:
  type: string
  description: "The type of the field. e.g. 'text', 'textarea', etc."
label:
  type: string
  description: "Field label which will appear on the EDIT page"
instructions:
  type: string
  description: "Instructions for authors. Shown when submitting data."
required:
  type: boolean
  default: true
wrapper_width:
  type: [integer, string]
wrapper_class:
  type: string
wrapper_id:
  type: string
wrapper:
  type: object
  properties:
    width:
      type: [integer, string]
    class:
      type: string
    id:
      type: string
conditional_logic: "@yaml/conditional-logic"
